@{
    ViewData["Title"] = "Home Page";
}
@* Prendo i campi dei department *@
@model Hospital.Models.SignUpCommonViewModel  

@* Lista di Dipartimenti presenti nel sistema, ciascuno con un colore diverso *@
@{
    var departmentColors = new Dictionary<Department, string>
    {
        { Department.Cardiology, "bg-danger" },      // rosso
        { Department.Neurology, "bg-primary" },      // blu
        { Department.Pediatrics, "bg-warning" },     // giallo
        { Department.Emergency, "bg-success" },      // verde
        { Department.General, "bg-info" }            // azzurro
    };
}


<!DOCTYPE html>
<html lang="en">
<head>
    <!-- fullCalendar -->
    <link rel="stylesheet" href="~/plugins/fullcalendar/main.css">
</head>


<body class="hold-transition">
    <!-- Small boxes (Stat box) -->
    <div class="row">
        <div class="col-lg-3 col-6">
            <!-- small box -->
            <div class="small-box bg-info">
                <div class="inner">
                    @* a <!----> roberto*@
                    @{
                        <script>
						let count = 0;
                        count = fetch('/api/getAllDoctor').count()
                                    .then(res => res.json())
                                    .then(data => {
                                        document.querySelector('.small-box.bg-info .inner h3').innerText = data.length;
                                    });
                        </script>
						// Inserisco il numero di dottori
						<h3>count</h3>
                    }
                    <p>Total Doctors</p>
                </div>
                <div class="icon">
                    <i class="ion ion-bag"></i>
                </div>
                <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
            </div>
        </div>
        <!-- ./col -->
        <div class="col-lg-3 col-6">
            <!-- small box -->
            <div class="small-box bg-success">
                <div class="inner">
                    <h3>53<sup style="font-size: 20px">%</sup></h3>

                    <p>Total Nurses</p>
                </div>
                <div class="icon">
                    <i class="ion ion-stats-bars"></i>
                </div>
                <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
            </div>
        </div>
        <!-- ./col -->
        @* <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-warning">
            <div class="inner">
                <h3>44</h3>

                <p>User Registrations</p>
            </div>
            <div class="icon">
                <i class="ion ion-person-add"></i>
            </div>
            <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
        </div>
    </div> *@
        <!-- ./col -->
        @* <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-danger">
            <div class="inner">
                <h3>65</h3>

                <p>Unique Visitors</p>
            </div>
            <div class="icon">
                <i class="ion ion-pie-graph"></i>
            </div>
            <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
        </div>
    </div> *@
        <!-- ./col -->
    </div>
    <!-- /.row -->

    <!-- /.row (main row) -->
    <div class="wrapper">
            <!-- Main content -->
            <section class="content">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-md-3">
                            <div class="sticky-top mb-3">
                                <!-- Departments Tag -->
                                <div class="card">
                                    <div class="card-header">
                                        <h4 class="card-title">Departments</h4>
                                    </div>
                                    <div class="card-body">
                                        <!-- the events -->
                                    <div id="external-events">
                                        @foreach (var department in Enum.GetValues(typeof(Department)))
                                        {
                                            <button type="button"
                                                    class="btn btn-sm text-white w-100 mb-2 @departmentColors[(Department)department]"
                                                    data-department="@department"
                                                    style="font-size: 0.9rem; padding: 0.4em 0.8em; border-radius: 0.25rem; letter-spacing: 0.5px;">
                                                @department
                                            </button>
                                        }
                                    </div>
                                    </div>
                                    <!-- /.card-body -->
                                </div>

                                <!-- Appointment Detail-->
                                <div class="card" id="appointment-detail-card" style="display:none;">
                                    <div class="card-header">
                                        <h3 class="card-title">Appointment Detail</h3>
                                    </div>
                                    <div class="card-body">
                                        <p><strong>Doctor:</strong> <span id="detail-doctor"></span></p>
                                        <p><strong>Nurse:</strong> <span id="detail-nurse"></span></p>
                                        <p><strong>Patient:</strong> <span id="detail-patient"></span></p>
                                        <p><strong>Date:</strong> <span id="detail-date"></span></p>
                                        <p>
                                            <strong>Department:</strong>
                                            <span id="detail-department" style="font-weight: bold;"></span>
                                        </p>
                                    </div>
                                </div>                            
                            </div>
                        </div>
                        <!-- /.col -->
                        <div class="col-md-9">
                            <div class="card card-primary">
                                <div class="card-body p-0">
                                    <!-- THE CALENDAR -->
                                    <div id="calendar"></div>
                                </div>
                                <!-- /.card-body -->
                            </div>
                            <!-- /.card -->
                        </div>
                        <!-- /.col -->
                    </div>
                    <!-- /.row -->
                </div><!-- /.container-fluid -->
            </section>
        <!-- /.control-sidebar -->
    </div>
    <!-- ./wrapper -->
    <!-- jQuery -->
    <script src="~/plugins/jquery/jquery.min.js"></script>
    <!-- Bootstrap -->
    <script src="~/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery UI -->
    <script src="~/plugins/jquery-ui/jquery-ui.min.js"></script>
    <!-- AdminLTE App -->
    <script src="~/dist/js/adminlte.min.js"></script>
    <!-- fullCalendar 2.2.5 -->
    <script src="~/plugins/moment/moment.min.js"></script>
    <script src="~/plugins/fullcalendar/main.js"></script>
    <!-- AdminLTE for demo purposes -->
    <script src="~/dist/js/demo.js"></script>
    <!-- Page specific script -->
    <script>
        $(function () {
          /* initialize the calendar
           -----------------------------------------------------------------*/
          var Calendar = FullCalendar.Calendar;
          var calendarEl = document.getElementById('calendar');

          const departmentColors = {
             Cardiology: '#e74c3c',    // rosso
             Neurology: '#3498db',     // blu
             Pediatrics: '#f1c40f',    // giallo
             Emergency: '#e67e22',     // arancione
             General: '#2ecc71'        // verde
          };

          // Quando avrò i dati:
          // fetch('/api/Appointments')
          //   .then(res => res.json())
          //   .then(data => {
          //     calendar.addEventSource(data.map(item => ({
          //       id: item.id,
          //       title: `${item.patientUsername} - ${item.doctorUsername}${item.nurseUsername ? " - " + item.nurseUsername : ""}`,
          //       start: item.date,
          //       backgroundColor: departmentColors[item.department] || '#999',
          //       borderColor: departmentColors[item.department] || '#999'
          //   })));
          // });


          const mockAppointments = [
            {
                id: 1,
                patientUsername: "mario.rossi",
                doctorUsername: "dr.bianchi",
                nurseUsername: "nurse.lucia",
                date: "2025-09-08T09:30:00",
                department: "Cardiology" // l'appuntamento stesso specifica il dipartimento
            },
            {
                id: 2,
                patientUsername: "giulia.verdi",
                doctorUsername: "dr.neri",
                nurseUsername: null,
                date: "2025-09-08T11:00:00",
                department: "Neurology"
            }
          ];

          var calendar = new Calendar(calendarEl, {
            headerToolbar: {
              left  : 'prev,next today',
              center: 'title',
              right : 'dayGridMonth,timeGridWeek,timeGridDay'
            },
            themeSystem: 'bootstrap',
            // Prepare events to fit into the 
            events: mockAppointments.map(item => ({
                id: item.id,
                title: `${item.patientUsername}`,
                start: item.date,
                backgroundColor: departmentColors[item.department || item.doctorDepartment] || '#999',
                borderColor: departmentColors[item.department || item.doctorDepartment] || '#999',
                extendedProps: {
                  doctorUsername: item.doctorUsername,
                  nurseUsername: item.nurseUsername,
                  patientUsername: item.patientUsername,
                  department: item.department
                }
            })),
            editable  : false,
            droppable : false, // this allows things to NOT be dropped onto the calendar !!!
                   // 🔹 Quando clicchi su un evento
            eventClick: function(info) {
              const event = info.event;
              const props = event.extendedProps;

              // Popola la card
              document.getElementById('detail-doctor').innerText = props.doctorUsername;
              document.getElementById('detail-nurse').innerText = props.nurseUsername || '—';
              document.getElementById('detail-patient').innerText = props.patientUsername;
              document.getElementById('detail-date').innerText = new Date(event.start).toLocaleString();

              const deptEl = document.getElementById('detail-department');
              deptEl.innerText = props.department;
              deptEl.style.color = departmentColors[props.department] || '#000';

              // Mostra la card
              document.getElementById('appointment-detail-card').style.display = 'block';
            }
          });

          calendar.render();
          // $('#calendar').fullCalendar()
        })
    </script>
</body>
</html>