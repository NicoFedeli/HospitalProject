{
    "ApiSettings": {
        // Base URL for the API
        "BaseUrl": "https://localhost:7240/api"  // Testa, al massimo metti http.
        // Si trova in HospitalAPI -> Properties -> launchSettings.json "https":"applicationUrl"
    },
    "Logging": {
        "LogLevel": {
          "Default": "Information",
          "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*"
}
